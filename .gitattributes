# Normalize line endings: LF in the repo for all text files
* text=auto eol=lf

# PowerShell scripts use CRLF on checkout (Windows-native)
*.ps1 text eol=crlf

# Treat these as binary (never normalized)
*.pt binary
*.pth binary
*.onnx binary
*.png binary
*.jpg binary
*.pdf binary
